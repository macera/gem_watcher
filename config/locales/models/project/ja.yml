ja:
  activerecord:
    models:
      project: プロジェクト

    attributes:
      project:
        name:              プロジェクト名
        description:       プロジェクト説明
        web_url:           WEBサイトURL
        http_url_to_repo:  リポジトリURL(HTTP)
        ssh_url_to_repo:   リポジトリURL(SSH)
